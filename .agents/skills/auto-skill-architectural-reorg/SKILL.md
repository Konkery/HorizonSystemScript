---
name: architectural-reorg
description: Перемещение и нумерация модульных каталогов проекта, корректировка импортов и обновление инструкций.
source: auto-skill
extracted_at: '2026-06-17T17:30:00Z'
---

## Подход
1. **Анализ текущей структуры** – перечисление каталогов `js/app` и проверка наличия файлов `init-orchestrator.js` и `README‑init‑orchestrator.md`.
2. **Создание префиксного каталога** `01__Common` и перемещение в него `Common`‑модулей, а также `init‑orchestrator.js` и его README.
3. **Корректировка импортов** внутри `init‑orchestrator.js` на новые относительные пути (`./config-loader` и т.д.).
4. **Нумерация остальных модулей** – переименование `LinuxScript → 02__LinuxScript`, `MongoDBscript → 03__MongoDBscript`, `NodeRedScript → 04__NodeRedScript` (переименование отложено до уточнения путей).
5. **Обновление инструкций** – добавить в `chat-google-system-script.md` правило нумерации каталогов и размещения исполняемых модулей/скриптов.
6. **Валидация** – проверка, что все файлы находятся по новым путям и импортов не вызывает ошибок; запуск `npm install` и линтинга.

Эта последовательность обеспечивает соблюдение архитектурных требований проекта без изменения бизнес‑логики, позволяя пользователям легко ориентироваться в порядке выполнения модулей.
