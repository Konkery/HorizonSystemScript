# Журнал описаний коммитов проекта 27/2025-N1-МАС

## Система нумерации

Коммиты структурируются по схеме: `[YYYYMMDD]-[Тип]-[Краткое описание]`
Нотация сообщений: [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0-beta.2/)

---

## 20260625-feat-implement-dynamic-configuration-and-standardize-infrastructure

### Commit Message:
`feat: implement dynamic configuration and standardize infrastructure for Horizon Automation`

### Детальное описание изменений:

Этот коммит фиксирует масштабную переработку как механизма конфигурации проекта так и его реализацию:

1. **Конфигурация:**
   - Новое имя файла конфигурации `settings_ha.json`.
   - Полная переработка структуры `settings_ha.json`: внедрена поддержка динамического количества инстансов платформы на одном Хабе.
2. **Исполняемые модули:**
   - Адаптация всех модулей (`Common`, `LinuxScript`, `MongoDBscript`, `NodeRedScript`) под новую схему данных конфигурации.
   - Реализация динамической генерации `systemd`-служб (скрипт `generate-services.js`).
   - Синхронизация логики управления пользователями и паролями между MongoDB, Node-Red и Linux Hub.
3. **Документация:**
   - Актуализация всех файлов `README-*.md` в соответствии с новой архитектурой.
   - Внедрение стандартов именования и документирования коммитов.

### Контекст:

Это ключевой этап подготовки платформы `Horizon Automation` к промышленной эксплуатации. Система переведена на рельсы «Инфраструктура как код» (IaC) с динамическим управлением параметрами через централизованный конфигурационный файл.

---

## 20260625-refactor-synchronize-project-and-update-standards

### Commit Message:
`refactor: синхронизировать состояние проекта и обновить стандарты оформления коммитов`

### Детальное описание изменений:
- Выполнена индексация всех файлов проекта для обеспечения 100% синхронности между локальной директорией и GitHub.
- Обновлены стандарты оформления коммитов (`git-convention.md`, `GEMINI.md`): переход на русский язык в описаниях коммитов.

---

## 20260625-fix-correct-hub-key-validation-in-config-loader

### Commit Message:
`fix: исправить валидацию ключей хабов в config-loader.js`

### Детальное описание изменений:
- Исправлена ошибка в методе `validate` класса `ConfigLoader`: изменен регистр ключей хабов (`hubLow`, `hubMid`, `hubHi`) в массиве `hubs` для соответствия ключам в `settings_ha.json`.
- Проведена успешная валидация загрузки конфигурации (Этап 0 и Этап 1 плана тестирования).

### Контекст:
Это первый этап тестирования системы после реализации масштабного рефакторинга инфраструктуры (коммит: `feat: implement dynamic configuration and standardize infrastructure for Horizon Automation`).
